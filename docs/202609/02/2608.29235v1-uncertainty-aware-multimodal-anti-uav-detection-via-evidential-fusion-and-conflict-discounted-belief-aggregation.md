---
title: Uncertainty-Aware Multimodal Anti-UAV Detection via Evidential Fusion and Conflict-Discounted Belief Aggregation
title_zh: 基于证据融合与冲突折扣信念聚合的不确定性感知多模态反无人机检测
authors: "Sharanda Suttorp, Seyed Sahand Mohammadi Ziabari, Ali Mohammed Mansour Alsahag"
date: 2026-08-29
pdf: "https://arxiv.org/pdf/2608.29235v1"
tags: ["query:q5"]
score: 6.5
evidence: 用于不确定性感知风险检测的证据深度学习
tldr: 为应对遮挡、快速运动等导致的传感器退化，本文将EDL扩展到RGB-热成像多模态，通过折扣信念融合将跨模态冲突转为不确定性并以低不确定性模态定框；在Anti-UAV上实时提升精度，但冲突近零使DBF与均值几乎无差，并给出校准与失配诊断。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.29235v1/fig-001.webp\", \"caption\": \"Figure 4: Reliability diagrams on the validation set (mean ± std over seeds 0-2). The gap between each bar and the diagonal equals the per-bin calibration error; ECE is the weighted mean gap. All methods concentrate mass in two bins: a midconfidence cluster (≈ 0.55) from harder frames where the detector confidence is lower, and a high-confidence cluster (≈ 0.95 − 1.0) from the dominant presence frames. At the mid-confidence bin, IR is overconfident (bar below diagonal), while fusion operators are underconfident (bar above diagonal); RGB is best calibrated there. Naive exhibits an additional bin near 0.75 from missing-detection frames encoded with 𝑢 ≈ 0.5 (Table 4). DBF and ABF are identical.\", \"page\": 6, \"index\": 1, \"width\": 506, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-002.webp\", \"caption\": \"Figure 3: Sparsification curves on the test set (signal: −?̂?2, mean ± std over seeds 0-2). Each curve shows the gap between an IoU-ordered oracle and the model when frames are removed in order of decreasing spatial uncertainty. Area under this gap is AUSE𝜎 (lower is better). IR’s Gaussian head is a substantially weaker localization failure predictor across the retention range (AUSE 0.0615), reflecting its weaker spatial uncertainty signal (Table 5). All four fusion operators are indistinguishable (AUSE = 0.0497), confirming the near-zero inter-modal conflict reported in Table 2. DMS is marginally worse (0.0517).\", \"page\": 6, \"index\": 2, \"width\": 506, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-003.webp\", \"caption\": \"Figure 8: RGB-only configuration. Histograms of the three candidate switching signals across DETECT (top row) and TRACK (bottom row) modes on the validation set. Each subcaption reports the AUROC of the signal as a failure predictor.\", \"page\": 15, \"index\": 3, \"width\": 1056, \"height\": 515}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-004.webp\", \"caption\": \"Figure 7: IR-only configuration. Histograms of the three candidate switching signals across DETECT (top row) and TRACK (bottom row) modes on the validation set. Each subcaption reports the AUROC of the signal as a failure predictor.\", \"page\": 15, \"index\": 4, \"width\": 1056, \"height\": 515}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-005.webp\", \"caption\": \"Table 1: Localization performance and benchmark comparability (mean ± std over seeds 0–2). AP@0.5 and mAP@[0.5:0.95] are the primary detection metrics; mIoU is mean IoU over present frames; Acc is the EDTC accuracy metric included for comparability with the original thermal-only baselines (det+track); AUSE𝜎 is the sparsification error using −?̂?2 𝑏𝑒𝑠𝑡 (lower is better). Bold = best within each group per split.\", \"page\": 7, \"index\": 5, \"width\": 1047, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-006.webp\", \"caption\": \"Figure 6: Multimodal uncertainty-aware detection-tracking framework. Both the global detection branch (YOLOv5s) and local tracking branch (SiamCAR) process RGB and infrared streams independently, each producing per-modality semantic uncertainty via an evidential head and spatial uncertainty via a Gaussian head. Semantic predictions are aggregated across modalities via Discounted Belief Fusion (DBF). Spatial predictions are resolved by selecting the bounding box from the loweruncertainty modality (Dynamic Modality Selection). State-dependent switching between detection and tracking requires both semantic (𝑢fused) and spatial (?̂?2 best) confidence to satisfy their respective thresholds simultaneously.\", \"page\": 14, \"index\": 6, \"width\": 532, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-007.webp\", \"caption\": \"Figure 5: Distribution of log ?̂?2 best conditioned on localization outcome (success: 𝐼𝑜𝑈 ≥ 0.5, failure: 0 < 𝐼𝑜𝑈 < 0.5) for IR-only (left) and RGB-only (right) configurations, pooled across seeds 0–2 on the validation set. IR shows heavily overlapping distributions (overlap coefficient 0.800, AUROC 0.598), while RGB shows clearly separated distributions (overlap 0.552, AUROC 0.760). The contrast confirms that IR’s Gaussian head produces outcome-insensitive variance, explaining the reversal of the spatial vs. semantic uncertainty ordering observed in Table 5.\", \"page\": 13, \"index\": 7, \"width\": 740, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-008.webp\", \"caption\": \"Table 9: Target-size-stratified discriminability of the spatial uncertainty ?̂?2 best for the IR-only andRGB-only detectors, on detected present frames (IoU> 0; success: IoU≥ 0.5, failure: 0 < IoU< 0.5), validation set, seeds 0–2 pooled. Frames are divided into three equal-sized groups by normalized predicted target size (𝑤 ·ℎ)/𝐴frame, with group boundaries computed per modality. OVL is the histogram overlap coefficient of log ?̂?2 best between the success and failure distributions (1 = indistinguishable, 0 = disjoint); AUROC scores ?̂?2 best as a failure predictor. The IR−RGB gap is constant across sizes, isolating the thermal modality rather than target size as the cause.\", \"page\": 13, \"index\": 8, \"width\": 606, \"height\": 199}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-009.webp\", \"caption\": \"Table 3: Fused-belief quality, evaluated independently of the box output. Presence-discriminationmetrics use the fused semantic belief only (IoU-free) under heavy class imbalance (val ≈44:1, test ≈73:1 present:absent). ECE measures calibration of the belief values; AUROC measures failure (misclassification) discrimination. DMS produces no fused belief and is not assessed here. Results are mean± std over seeds 0-2. Bold = best per column within each split.\", \"page\": 8, \"index\": 9, \"width\": 926, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-010.webp\", \"caption\": \"Figure 2: Multimodal uncertainty-aware detection framework. The detection branch (YOLOv5s) processes RGB and infrared streams independently, each producing per-modality semantic uncertainty via an evidential head and spatial uncertainty via a Gaussian head. Semantic opinions are aggregated across modalities via Discounted Belief Fusion (DBF), and the bounding box is resolved by selecting the lowerspatial-uncertainty modality (Dynamic Modality Selection). The fused opinion is passed through an uncertainty-gated output decision: a box is emitted only when the fused belief favors the UAV class (𝑏fused (UAV) > 𝑏fused (bg)) and the fused uncertainty falls below the gate threshold (𝑢fused < 𝜃cls); otherwise the target is reported absent.\", \"page\": 4, \"index\": 10, \"width\": 506, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-011.webp\", \"caption\": \"Table 8: List of abbreviations used throughout this paper.\", \"page\": 12, \"index\": 11, \"width\": 502, \"height\": 820}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-012.webp\", \"caption\": \"Table 6: Attribute-based robustness on the Anti-UAV test set. Means over seeds 0-2. 𝜌 is Spearman correlation between per-frame 𝑢fused and IoU (more negative = uncertainty better tracks localization failure). 𝑢 is mean 𝑢fused over all frames (including non-detections), reported as a diagnostic of each method’s uncertainty scale (not a quality metric; not scored). Best per row within each scored metric group in bold.\", \"page\": 9, \"index\": 12, \"width\": 1012, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2608.29235v1/fig-013.webp\", \"caption\": \"Figure 9: DBF configuration. Histograms of the three candidate switching signals across DETECT (top row) and TRACK (bottom row) modes on the validation set. Each subcaption reports the AUROC of the signal as a failure predictor.\", \"page\": 16, \"index\": 13, \"width\": 1056, \"height\": 515}]"
motivation: 多传感器反无人机感知在遮挡、快速运动或模态失效下需具备不确定性感知与冲突表达，现有方法缺乏此能力。
method: 将EDTC扩展为RGB-热多模态EDL框架，采用折扣信念融合把跨模态冲突折算为不确定性并聚合分类意见，检测框由低不确定性模态给出。
result: 在Anti-UAV上融合优于单模态（Acc 0.670 vs 0.604/0.598），实时≥38 FPS，置信校准良好（ECE 0.057），但定位异常检测弱于空间方差（AUROC 0.626 vs 0.739）。
conclusion: 由于数据集以目标存在为主且缺失编码空洞，跨模态冲突近零致DBF与无折扣平均近似等效，提示冲突感知融合仅在存在显著分歧时才有益。
---

## 摘要
反无人机感知系统必须在遮挡、快速运动或模态特定故障导致的传感器数据流退化时仍保持可靠性。现有多模态反无人机系统以确定性方式融合 RGB 与热成像数据流，不对预测不确定性进行建模，因而在各流意见不一致时无法表达疑虑。证据深度学习（EDL）可在一次前向推理中产生经校准的逐类别不确定性。EDTC 已将其用于仅热成像的感知，但跨模态的证据融合尚未解决。本文通过折扣信念融合（DBF）将 EDTC 扩展至 RGB–热成像的多模态感知；DBF 在聚合各数据流意见之前，将跨模态冲突转化为不确定性质量。对于边界框，通过选择不确定性更低的模态进行裁决。在 Anti-UAV 基准上，多模态融合在实时速度（至少 38 FPS）下持续优于任何单一数据流（测试准确率 0.670，对比 IR 0.604、RGB 0.598）。然而，DBF 的经验表现与未折扣的平均几乎不可区分：在这个以“存在”为主导的基准上，跨模态冲突近乎为零，使折扣步骤失去作用。融合后的不确定性具有良好校准（ECE 0.057），但如预期地，作为定位失败检测器弱于空间方差（AUROC 0.626 对 0.739）。这一无效结果具有结构性：该基准中几乎普遍的存在标签与空洞的漏检编码共同抑制了跨模态冲突，这一诊断界定了冲突感知融合能提供可测量收益的适用范围。

## Abstract
Anti-UAV perception systems must remain reliable when sensor streams degrade under occlusion, fast motion, or modality-specific failure. Existing multimodal anti-UAV systems fuse RGB and thermal streams deterministically, without modeling predictive uncertainty, and cannot express doubt when streams disagree. Evidential Deep Learning (EDL) produces calibrated per-class uncertainty in a single forward pass. EDTC already exploits this for thermal-only perception, yet cross-modal evidential fusion remains unaddressed.   This paper extends EDTC to multimodal RGB-Thermal perception via Discounted Belief Fusion (DBF), which converts inter-modal conflict into uncertainty mass before aggregating stream opinions. Bounding boxes are resolved by selecting the lower-uncertainty modality. On the Anti-UAV benchmark, multimodal fusion consistently outperforms either single stream (test Acc 0.670 vs. 0.604 IR, 0.598 RGB) at real-time speed (at least 38 FPS). However, DBF is empirically indistinguishable from undiscounted averaging: near-zero inter-modal conflict on this presence-dominated benchmark leaves the discounting step inert. The fused uncertainty is well-calibrated (ECE 0.057) yet expectedly a weaker localization failure detector than spatial variance (AUROC 0.626 vs. 0.739). The null result is structural: the benchmark's near-universal presence and vacuous miss-encoding jointly suppress inter-modal conflict, a diagnosis that delimits where conflict-aware fusion provides measurable benefit.